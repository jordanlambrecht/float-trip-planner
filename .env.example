# Env vars are managed in Doppler (project: niobrara), not .env files.
# One-time setup: doppler login && doppler setup --no-interactive
# Required secrets: DATABASE_URL (Postgres connection string, SSL)
